npm install react-router-dom

npm install aos --save

pip install sqlalchemy

pip install passlib[bcrypt]

pip install fastapi

pip install uvicorn

npm install aos --save

python -m uvicorn main:app --reload --port 8080

python odpalany w backend, npm we froncie
